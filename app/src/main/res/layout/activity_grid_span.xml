<?xml version="1.0" encoding="utf-8"?>
<GridLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:columnCount="3"
    android:rowCount="2"
    android:padding="16dp">

    <!-- Кнопка 1: занимает 2 колонки (первая строка, колонки 0 и 1) -->
    <Button
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_columnWeight="1"
        android:layout_rowWeight="1"
        android:layout_columnSpan="2"
        android:layout_gravity="fill"
        android:text="Кнопка 1 (2 колонки)"
        android:layout_margin="4dp"/>

    <!-- Кнопка 2: стоит в первой строке, колонке 2 (справа) -->
    <Button
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_columnWeight="1"
        android:layout_rowWeight="1"
        android:layout_column="2"
        android:text="Кнопка 2"
        android:layout_margin="4dp"/>

    <!-- Кнопка 3: занимает все 3 колонки во второй строке -->
    <Button
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_columnWeight="1"
        android:layout_rowWeight="1"
        android:layout_row="1"
        android:layout_columnSpan="3"
        android:layout_gravity="fill"
        android:text="Кнопка 3 (3 колонки)"
        android:layout_margin="4dp"/>

</GridLayout>